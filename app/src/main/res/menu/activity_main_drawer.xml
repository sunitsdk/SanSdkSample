<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_native"
            android:icon="@drawable/ic_menu_send"
            android:title="@string/menu_native" />

        <item
            android:id="@+id/nav_banner"
            android:icon="@drawable/ic_menu_send"
            android:title="@string/menu_banner" />

        <item
            android:id="@+id/nav_rewarded"
            android:icon="@drawable/ic_menu_send"
            android:title="@string/menu_rewarded" />
        <item
            android:id="@+id/nav_interstitial"
            android:icon="@drawable/ic_menu_send"
            android:title="@string/menu_interstitial" />
    </group>
</menu>